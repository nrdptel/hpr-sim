# The Python environment for the code-to-code oracles (RocketPy, and OpenRocket through JPype).
# `cargo xtask refs fetch` installs it into the gitignored refs/venv with `uv sync --locked`,
# which installs exactly what uv.lock pins and checks every artifact's hash. After editing this
# file, run `uv lock --project validation/oracles` and commit both files.
[project]
name = "hpr-sim-oracles"
version = "0.0.0"
description = "Reference simulators run as external oracles for hpr-sim validation (not a package)."
requires-python = ">=3.12,<3.13"
dependencies = [
    # The primary code-to-code oracle (MIT).
    "rocketpy==1.13.0",
    # Starts the JVM for the OpenRocket oracle (Apache-2.0). How M2.2 drives the jar through it
    # is still open; until then nothing GPL is installed here.
    "jpype1>=1.7.1",
    # Arbitrary-precision arithmetic (BSD-3-Clause) for reference values computed from published
    # formulas, such as WGS 84 normal gravity.
    "mpmath==1.3.0",
    # An independent implementation of the 1976 US Standard Atmosphere (Apache-2.0), cross-checked
    # against the transcribed tables.
    "ambiance==1.3.1",
    # The Unidata netCDF library's Python interface (MIT), already RocketPy's: writes the netCDF
    # files the reader is checked against and reads them back (`validation/oracles/netcdf/`).
    "netCDF4==1.7.4",
    # Labelled arrays (Apache-2.0): runs the guide's conversion of a netCDF-4 ERA5 file to the
    # 64-bit offset format, exactly as a user would (`validation/oracles/netcdf/era5.py`).
    "xarray==2026.7.0",
]

[tool.uv]
package = false
# `refs` relies on `uv sync --locked --check`.
required-version = ">=0.12"

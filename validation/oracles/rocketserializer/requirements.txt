# The environment `geometry.py` runs in (ADR-059), every package pinned. Install it with
#
#   uv venv -p 3.11 refs/venv-rs
#   uv pip install -p refs/venv-rs --no-deps -r validation/oracles/rocketserializer/requirements.txt
#
# `--no-deps` matters: RocketSerializer declares `orhelper` (GPL-2.0), which pins JPype below 1.5,
# but the extractors this script calls need only NumPy, BeautifulSoup and lxml, so `orhelper` is
# never installed. JPype starts OpenRocket, as `validation/oracles/openrocket/` does.
rocketserializer @ git+https://github.com/RocketPy-Team/RocketSerializer@66d8ca8c9be36816c4157fbdf249e87fb8c1f5dc
numpy==2.4.6
beautifulsoup4==4.15.0
soupsieve==2.9.2
typing-extensions==4.16.0
lxml==6.1.3
jpype1==1.7.1
packaging==26.3
